using Fsi.DataSystem.Libraries;
using UnityEngine;

namespace [Namespace]
{
	public class [Name]LibraryDrawerUsageExample : MonoBehaviour
	{
		// LibraryAttributeDrawer is shared; no per-library drawer is required.
		[Library(typeof([Name]Settings), "Library")]
		public [Data] entry;
	}
}
